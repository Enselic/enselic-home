git --no-pager log GIMP_2_6_1.. -- `find -maxdepth 1 -type d | sort | grep --invert-match "^./po" | xargs echo` | grep "Author:" | uniq > authors.txt


tarball=SDL-1.2.14.tar.gz; tar -xf $tarball && cd ${tarball%.tar.*} && ./configure --prefix=/home/buildbot/usr && make -j3 && make install


sudo smartctl -a /dev/sda | grep Load_Cycle_Count


Things to remember:
 * Use the Inconsolata font by Ralp Levien for code

